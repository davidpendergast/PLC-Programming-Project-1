; state.scm

(define remove
  (lambda (var s)))

(define declare
  (lambda (var s)

(define assign
  (lambda (var value s)

(define get
  (lambda (var s)

(define assigned?
  (lambda (var s)

(define declared?
  (lambda (var s)
    
    